queue(start)
visited(start)
step = -1
while queue:
  step++
  for in queue.size:
    pop()
    for nodes:
      calculate
      if next == target: return
      if outofbounds  or visited (or more): continue
      queue.push
      visited.add
